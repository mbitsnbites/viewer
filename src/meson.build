project('Viewer', ['cpp', 'c'])

# Add third party libraries.
subdir('third_party')

# All includes are relative to the root directory.
root_inc = include_directories('.')

# Add project internal libraries.
subdir('base')
subdir('gfx')
subdir('ui')

# Add viewer porject(s).
subdir('viewer')

