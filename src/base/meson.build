base_sources = ['error.cc',
                'error.h',
                'make_unique.h']

base_lib = library('base',
                   base_sources,
                   include_directories: root_inc)

base = declare_dependency(link_with: base_lib)

