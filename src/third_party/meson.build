# GLFW
# Note: We would like to build GLFW from source, but until it supports meson
# out of the box, we'll depend on system libraries instead.
glfw = dependency('glfw3', required: true)

# gl3w
subdir('gl3w')

# ImGui
subdir('imgui')

